#pragma once

#define YAML_CPP_STATIC_DEFINE

// Standard C++
#include <vector>
#include <string>
#include <fstream>
#include <optional>
#include <iostream>
#include <algorithm>
#include <filesystem>
#include <stacktrace>
#include <functional>
#include <string_view>
#include <unordered_map>

// Other libs installed in compiler (Installed in the compiler via package manager)
#include <yaml-cpp/yaml.h>

// Other libs:
#include "stb/stb_image.h"